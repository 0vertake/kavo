// Package version holds the kavo build version, set via -ldflags at release time.
package version

// Version is the kavo version string reported by binaries and the S3 API Server header.
var Version = "dev"
